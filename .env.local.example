# Base URL of the resume-saas backend API.
# Local dev: run `docker compose up -d` + `uvicorn app.main:app --reload` in the
# resume-saas repo, then point this at it.
NEXT_PUBLIC_API_URL=http://localhost:8000
